// TODO: this is a local setting. It should be changed in the future

module.exports = {
  host: 'localhost', // Should be changed 
  port: 3016,        // Should be changed 
  protocol: 'http'   // http / https
}